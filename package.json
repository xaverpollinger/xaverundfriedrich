{
  "name": "xaverfriedrich",
  "version": "1.0.0",
  "description": "Portfolio website — xaverfriedrich.de",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "rm -rf _site && npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^6.0.4",
    "markdown-it": "^14.1.1"
  }
}
